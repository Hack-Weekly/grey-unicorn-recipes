<p style="color: green"><%= notice %></p>

<%= render @recipe %>

<div>
  <%= link_to "Edit this recipe", edit_recipe_path(@recipe) %> |
  <%= link_to "Back to recipes", root_path %>
  <%= link_to fa_icon('trash'), @recipe, data: { turbo_method: :delete,
                        turbo_confirm: 'Are you sure?' }, class: "blogcrumb" %>
</div>

